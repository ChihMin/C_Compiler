const int y = 123;
int g;
int f(int x, int y) { 
    int x = 10;
    switch (x) {
    case 1:
    case 'x':
        if (x > 10) {
            x = x +1;
        } else {
            y = y + 2;
        }
    default:
        for (;;) {
            switch(x) {
            case 'r':
                for (;;) {
                    switch(x) {
                    case 'r':
    switch (x) {
    case 1:
    case 'x':
        if (x > 10) {
            x = x +1;
        } else {
            y = y + 2;
        }
    default:
        for (;;) {
            switch(x) {
            case 'r':
                for (;;) {
                    switch(x) {
                    case 'r':
                    case 'g':
    switch (x) {
    case 1:
    case 'x':
        if (x > 10) {
            x = x +1;
        } else {
            y = y + 2;
        }
    default:
        for (;;) {
            switch(x) {
            case 'r':
                for (;;) {
                    switch(x) {
                    case 'r':
                    case 'g':
                    }
                }
            case 'g':
            }
        }
        switch(x) {
        case 'r':
        case 'g':
        }
    }
                    }
                }
            case 'g':
            }
        }
        switch(x) {
        case 'r':
        case 'g':
        }
    }
                    case 'g':
    switch (x) {
    case 1:
    case 'x':
        if (x > 10) {
            x = x +1;
        } else {
            y = y + 2;
        }
    default:
        for (;;) {
            switch(x) {
            case 'r':
                for (;;) {
                    switch(x) {
                    case 'r':
                    case 'g':
                    }
                }
            case 'g':
            }
        }
        switch(x) {
        case 'r':
        case 'g':
        }
    }
                    }
                }
            case 'g':
            }
        }
        switch(x) {
        case 'r':
        case 'g':
        }
    }
}
int a[12][1] = {123};
int x = a[12][(2*3+1)] + 5;

int f(int x, int y) { 
    int x = 10;
    switch (x) {
    case 1:
    case 'x':
        if (x > 10) {
            x = x +1;
        } else {
            y = y + 2;
        }
    default:
        for (;;) {
            switch(x) {
            case 'r':
                for (;;) {
                    switch(x) {
                    case 'r':
    switch (x) {
    case 1:
    case 'x':
        if (x > 10) {
            x = x +1;
        } else {
            y = y + 2;
        }
    default:
        for (;;) {
            switch(x) {
            case 'r':
                for (;;) {
                    switch(x) {
                    case 'r':
                    case 'g':
    switch (x) {
    case 1:
    case 'x':
        if (x > 10) {
            x = x +1;
        } else {
            y = y + 2;
        }
    default:
        for (;;) {
            switch(x) {
            case 'r':
                for (;;) {
                    switch(x) {
                    case 'r':
                    do {
    switch (x) {
    case 1:
    case 'x':
        if (x > 10) {
            x = x +1;
        } else {
            y = y + 2;
        }
    default:
        for (;;) {
            switch(x) {
            case 'r':
                for (;;) {
                    switch(x) {
                    case 'r':
    switch (x) {
    case 1:
    case 'x':
        if (x > 10) {
            x = x +1;
        } else {
            y = y + 2;
        }
    default:
        for (;;) {
            switch(x) {
            case 'r':
                for (;;) {
                    switch(x) {
                    case 'r':
                    case 'g':
    switch (x) {
    case 1:
    case 'x':
        if (x > 10) {
            x = x +1;
        } else {
            y = y + 2;
        }
    default:
        for (;;) {
            switch(x) {
            case 'r':
                for (;;) {
                    switch(x) {
                    case 'r':
                    do {
                    } while(0);
                    case 'g':
                    }
                }
            case 'g':
            }
        }
        switch(x) {
        case 'r':
        case 'g':
        }
    }
                    }
                }
            case 'g':
            }
        }
        switch(x) {
        case 'r':
        case 'g':
        }
    }
                    case 'g':
    switch (x) {
    case 1:
    case 'x':
        if (x > 10) {
            x = x +1;
        } else {
            y = y + 2;
        }
    default:
        for (;;) {
            switch(x) {
            case 'r':
                for (;;) {
                    switch(x) {
                    case 'r':
                    case 'g':
                    }
                }
            case 'g':
            }
        }
        switch(x) {
        case 'r':
        case 'g':
        }
    }
                    }
                }
            case 'g':
            }
        }
        switch(x) {
        case 'r':
        case 'g':
        }
    }
                    } while(0);
                    case 'g':
                    }
                }
            case 'g':
            }
        }
        switch(x) {
        case 'r':
        case 'g':
        }
    }
                    }
                }
            case 'g':
            }
        }
        switch(x) {
        case 'r':
        case 'g':
        }
    }
                    case 'g':
    switch (x) {
    case 1:
    case 'x':
        if (x > 10) {
            x = x +1;
        } else {
            y = y + 2;
        }
    default:
        for (;;) {
            switch(x) {
            case 'r':
                for (;;) {
                    switch(x) {
                    case 'r':
                    case 'g':
                    }
                }
            case 'g':
            }
        }
        switch(x) {
        case 'r':
        case 'g':
        }
    }
                    }
                }
            case 'g':
            }
        }
        switch(x) {
        case 'r':
        case 'g':
        }
    }
}
